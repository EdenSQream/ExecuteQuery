src.dir=java/src
build.dir=build
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java
dist.dir=dist
dist.jar=${dist.dir}/eq.jar
dist.files=package-files
libs.dir=lib
jar.includes=org/**
manifest.file=manifest.mf
main.class=org.executequery.ExecuteQuery

